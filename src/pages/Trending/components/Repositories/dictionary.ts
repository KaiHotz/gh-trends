import { createDictionary } from '../../../../utils';

export const messages = createDictionary('repositories', {
  star: 'Star',
  sponsor: 'Sponsor',
  build_by: 'Build by',
  starts_today: 'stars today',
});
